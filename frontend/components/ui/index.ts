/**
 * components/ui/index.ts
 *
 * Barrel export for the UI component library (Button, Card, Input,
 * Badge, ProgressBar, etc.) — implementations live in `./components`.
 *
 * The Zustand UI store (theme, sidebar, upgrade modal, toasts) lives in
 * `@/store/ui`, alongside the other app stores.
 */
export * from './components';
