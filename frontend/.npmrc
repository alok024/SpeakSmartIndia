package-manager=npm
# @cloudflare/next-on-pages peer dep caps next at <=15.5.2 but hasn't released
# a version supporting newer patches yet — suppress the warning, build works fine
legacy-peer-deps=true
